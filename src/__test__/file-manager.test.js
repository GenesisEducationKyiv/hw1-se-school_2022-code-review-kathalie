import {jest} from '@jest/globals';
import fs from 'fs';

// import * as fileManager from '../subscriptions-manager.js';

jest.mock('fs');

describe('when adding text to file', () => {
    it('appends to the file', () => {

    });
});